// Ensure sc_user_employeeGuid is defined
const sc_user_employeeGuid = window.sc_user_employeeGuid;

// Construct the message
const message = `Hello World ${sc_user_employeeGuid}`;

// Print to console
console.log(message);

// Show an alert
alert(message);
